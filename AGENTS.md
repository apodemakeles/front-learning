# AGENTS.md — 前端学习课程仓库 · 工作指南

> 本文件是所有参与本仓库的 AI 助手的工作指南。每次会话开工先读这里，再去 `wayfinder/map.md` 看进度与下一步工单。

## 仓库使命

为一名有 20 年经验的后端开发者（Java 背景）量身定制一套**原创、详尽、可实操**的现代前端课程。课程直接从框架与工程化切入，最终目标：学员能**独立完成**前端开发、构建、部署、上线全流程。本仓库托管在 GitHub，本身就是学员的课程页面。

## 学员画像（写作时时刻对照）

- 20 年后端经验：Java / Spring、Maven、HTTP、数据库、微服务、Linux、Docker 都熟
- 前端接近零基础，但**不需要**从 HTML / CSS / Ajax 基础教起——遇到相关概念用一两句话带过或给延伸链接
- 真实工作中要维护公司存量 Vue 2 项目、参与 Vue 3 项目
- npm / Node.js / 前端构建 / 部署 / 包依赖管理等工程知识是空白——这些是课程的**一等公民**，不是附录
- 善用后端类比能极大降低学习成本（见下文类比表）

## 内容原则

1. **从框架开始**：第一课就是现代前端全景与 Vue，不设 HTML/CSS/Ajax 基础课。
2. **教新版本，讲旧版本**：主线使用课程版本基准（见 `notes/tech-baseline.md`，基准时点 2026-09：Vue 3.5、Vite 8、Pinia 4、Vue Router 5、Element Plus 等）；存量技术（Vue 2、Vue CLI/Webpack、Vuex、Element UI、class 风格 TS 组件）以"读懂并维护存量代码"为目标设专题，不做完整平行教学。
3. **原创撰写**：所有课程文章原创。可以参考官方文档、优质社区资料的事实与示例思路，但讲解组织、代码示例、行文必须自己写，禁止成段搬运。
4. **由浅入深**：每课只聚焦一个主题，课程间依赖关系清晰，难度阶梯平缓；前面的课不为后面的课"剧透"复杂度。
5. **原理与用法配比**：常态课以"会用"为主（约七成），原理占三成；关键机制（响应式系统、模块标准、构建打包、路由、渲染）另设专门原理课深挖。取舍标准：这个原理是否影响学员"写出正确的代码、读懂报错、做对性能决策"。
6. **每课必有作业**：动手练习 + 明确的验收标准（做完应该看到什么）；作业尽量围绕贯穿项目迭代。

## 每课的标准结构

1. 本课目标——学完你能做什么（3 条以内）
2. 概念讲解——含后端类比
3. 动手实操——完整可运行的示例，一步一步
4. 原理深入——本课机制是如何工作的
5. 作业——含验收标准
6. 延伸阅读——官方文档等一手资料链接

## 常用后端类比表（讲解时优先使用）

| 前端概念 | 后端类比 |
|---|---|
| Node.js | JVM（运行时，不是框架） |
| npm / pnpm / yarn | Maven / Gradle（依赖管理 + 构建） |
| package.json | pom.xml |
| package-lock.json | 依赖锁定（确定性构建，类比 Maven 的版本锁定） |
| node_modules | 本地仓库（类比 ~/.m2/repository，但装在项目内） |
| semver 的 ^ 与 ~ | Maven 版本范围 |
| ESM import/export | Java 的 package / import（静态可分析） |
| Vite 开发服务器 | 热部署 + 按需编译 |
| Vite / Webpack 生产构建 | mvn package + shade（打包成可部署产物） |
| Vue 组件 | 自包含的视图单元（模板 + 逻辑 + 样式） |
| Pinia | 应用级共享状态（类比 Bean 的作用域问题） |
| 虚拟 DOM diff | 增量计算，避免全量重渲染 |
| SPA 路由 | 浏览器侧的 URL 分发器（网关路由的前端版） |
| devServer 代理 / CORS | 跨域信任与反向代理 |

## 写作规范

- 中文行文，技术名词保留英文；代码注释用中文
- 代码块必须标注语言；多文件示例标注文件路径；命令给出预期输出的关键行
- 涉及版本时注明"截至 YYYY-MM"
- 篇幅：单课正文以学员 45–90 分钟消化完为宜，宁可拆课，不可灌水

## 仓库结构（随课程推进演进）

```
README.md          课程主页与目录（学员入口）
AGENTS.md          本文件
lessons/           课程文章（按编号命名，如 lessons/01-xxx.md）
demos/             课程配套可运行示例
project/           贯穿项目（毕业项目）
notes/             参考笔记（版本基准、存量栈调研等）
wayfinder/         课程规划地图与决策工单（进度唯一事实源）
```

## 规划与推进机制（wayfinder）

- 课程整体规划、未决决策、进度看板：`wayfinder/map.md`（先读它，再选工单）
- 工单规约与 tracker 用法：`wayfinder/README.md`
- 每次会话至多解决一个工单（research 类除外）；grilling 工单需要学员在线一问一答推进
- 写课属于执行：在大纲定稿后以 task 工单形式纳入地图，每次会话推进一课

## Git / GitHub 约定

- 主分支 `main`；完成一课或一组课即提交，提交信息用中文
- 仓库发布在 GitHub 作为课程页；**仓库内容不得出现公司内部信息**（内网地址、内部仓库名、内部包源、服务器 IP 等），涉及存量项目一律用"公司存量项目"等泛称
