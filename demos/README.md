# demos/ —— 课程配套示例

组织约定：

- 每课一个目录：`demos/NN-slug/`，与 `lessons/NN-slug.md` 一一对应
- 示例自包含、可直接运行（脚手架级项目自带 package.json，小示例可为单文件）
- 运行方式在对应课程正文"动手实操"中给出
- 只提交源码，不提交 node_modules / dist 等构建产物（已被根 .gitignore 排除）
